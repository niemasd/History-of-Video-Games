This directory contains all companies. The following attributes are required:

* **`name`** — The anglicized name of the company as a string
* **`date_start`** — The start (i.e., founding) date of the company as a string in the format `YYYY-MM-DD`

The following attributes are optional:

* **`name_orig`** — The original name of the company (e.g. in its original language) as a string
* **`location_start`** — The initial (i.e., founding) location of the company as a string
* **`founders`** — A list containing the names of the founders of the company as strings
* **`date_end`** — The end (i.e., closing) date of the company as a string in the format `YYYY-MM-DD`
